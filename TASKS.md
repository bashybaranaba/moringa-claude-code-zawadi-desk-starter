# Breakout 1 Tasks

Pick one small task. Keep the diff under 30 lines.

## Dev A

1. Add a `description` field to project list responses.
2. Sort projects by `updatedAt` descending.
3. Add a repository helper named `findProjectOrThrow`.

## Dev B

1. Add a `subtitle` prop to `PageHeader`.
2. Add a `compact` variant to `ProjectCard`.
3. Make the empty state copy configurable.

## Stretch

Ask the agent a question the repo cannot answer. Save the exact wording that produced the bad answer.
